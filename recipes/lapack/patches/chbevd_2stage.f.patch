--- a/chbevd_2stage.f
+++ b/chbevd_2stage.f
@@ -345,7 +345,7 @@
       END IF
 *
       IF( INFO.EQ.0 ) THEN
-         WORK( 1 )  = CMPLX( LWMIN )
+         WORK( 1 ) = LWMIN 
          RWORK( 1 ) = REAL( LRWMIN )
          IWORK( 1 ) = LIWMIN
 *
@@ -447,7 +447,7 @@
          CALL SSCAL( IMAX, ONE / SIGMA, W, 1 )
       END IF
 *
-      WORK( 1 )  = CMPLX( LWMIN )
+      WORK( 1 ) = LWMIN 
       RWORK( 1 ) = REAL( LRWMIN )
       IWORK( 1 ) = LIWMIN
       RETURN
