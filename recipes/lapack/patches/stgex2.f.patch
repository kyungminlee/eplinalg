--- a/stgex2.f
+++ b/stgex2.f
@@ -286,9 +286,9 @@
       IF( N1.GT.N .OR. ( J1+N1 ).GT.N )
      $   RETURN
       M = N1 + N2
-      IF( LWORK.LT.MAX( N*M, M*M*2 ) ) THEN
+      IF( LWORK.LT.MAX( 1, N*M, M*M*2 ) ) THEN
          INFO = -16
-         WORK( 1 ) = REAL( MAX( N*M, M*M*2 ) )
+         WORK( 1 ) = REAL( MAX( 1, N*M, M*M*2 ) )
          RETURN
       END IF
 *
