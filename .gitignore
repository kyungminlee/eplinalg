# Build artifacts
build/
.cache/
compile_commands.json

# Python
__pycache__/
*.pyc
.venv/

# External source tarballs
external/*.tar.gz
external/*.tar.xz
external/*.dsc
external/openblas-*/
external/openblas/

# Bench JSON artifacts (run output, not source)
/bench_*.json

# Test-report JSON artifacts (per-target harness output, not source)
/*.kind*.json
/*.multifloats.json

# Work directories (migration output)
work/
output/
_build/
_lib/
# Session scratch (temporary scripts / downloads, not source) and the
# self-contained workspace/ handoff dir (its own nested git repo).
scratchpad/
workspace/
*.o
*.mod
# Committed kind10 stage snapshot was removed from tracking — it is
# regenerable build output (`migrator stage staged --target kind10`) and
# was drifting from the recipes. Treat like the other output dirs above.
staged/
external/multifloats/multifloats.f90

# Repro binaries (built locally)
repro/*
!repro/*.f90
!repro/*.md
!repro/patches/
